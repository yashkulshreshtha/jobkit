# docs / assets

Portfolio and documentation assets for JobKit (tracked in the public code repo — keep this
free of any personal profile data).

- **`screenshot.png`** — the hero image shown at the top of the main `README.md`. Capture the
  **Pipeline** or **Tailor** tab at a clean, readable width (~1400px wide works well on GitHub).
  Replace this file any time the UI changes.
